services:
  frontend:
    image: ${FRONTEND_IMAGE}
    ports:
      - "3001:8080"
    env_file:
      - .env
    restart: always

  backend:
    image: ${FRONTEND_IMAGE}
    ports:
      - "5001:4040"
    env_file:
      - .env
    restart: always
